# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# CI/CD artifacts (packaged ZIPs — never commit generated artifacts)
artifacts/

# Local environment files — never commit credentials or tokens
.env
.env.*

# Google Cloud / service account credential files
credentials.json
service-account*.json
*-service-account.json

# npm publish token file
.npmrc

# Generic secret files
*.secret
secrets.*

# Local OAuth token helper — never commit
scripts/get-cws-refresh-token.mjs

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
